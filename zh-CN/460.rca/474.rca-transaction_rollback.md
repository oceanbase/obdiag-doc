# 根因分析场景：事务回滚报错 transaction_rollback

## 适用场景
本文适用于事务回滚报错的根因分析,基于 observer 内部表数据以及日志进行分析。

目前支持 observer 4.0.0.0 及之后的版本。 

obdiag 使用前需要配置 ~/.obdiag/config.yml 或使用 --config 配置集群信息。

由于内部数据存在及时性，所以仅支持对当前时的数据进行分析。






## 使用示例

### 1. 默认执行

```shell
obdiag rca run --scene=transaction_rollback
```
