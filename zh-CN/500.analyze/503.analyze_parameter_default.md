# 参数分析(和默认值对比)

使用该命令可以一键分析 OceanBase 集群参数和默认值的差异，如果不一致将会输出差异结果。


<main id="notice" type='explain'>
  <h4>说明</h4>
  <p>需要确保已经在 obdiag 配置文件 <code>config.yml</code> 中配置好需要收集节点的登录信息。相关的详细配置介绍，参见 <a href="./300.obdiag-config-guide.md">obdiag 配置</a>。</p>
</main>

## 使用说明

```shell
obdiag analyze parameter default [option]
```

选项说明如下：

| 选项名                                                   | 是否必选 | 数据类型   | 默认值                       | 说明                                                                |
|---------------------|------|--------|---------------------------|-------------------------------------------------------------------|
| --file | 否    | string |   默认为空              | 参数文件 | observer 版本 < 4.2.2 时此参数文件必须要填写，传递的是之前执行的 `obdiag gather parameter` 的存档文件|
| --store_dir | 否    | string |   默认为命令执行的当前路径              | 存储结果的本地路径。 |
| -c | 否    | string |   `~/.obdiag/config.yml `           | 配置文件路径     |


## 使用示例

```bash
obdiag analyze parameter default
```
